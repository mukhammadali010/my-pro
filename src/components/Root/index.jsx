import React from 'react';
import Navbar from '../Navbar';
import Hero from '../Hero';
import Service from '../Service';

const Root = () => {
    return (
        <div>
            <Navbar/>
            <Hero/>
            <Service/>
        </div>
    );
};

export default Root;
